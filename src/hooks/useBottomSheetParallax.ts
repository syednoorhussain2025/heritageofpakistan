/**
 * Placeholder — bottom sheet parallax disabled.
 * Requires the page scroll container to be the scaled element (position:fixed)
 * to work correctly at any scroll position. Kept for future implementation.
 */
export function useBottomSheetParallax(_active: boolean) {
  // no-op
}
